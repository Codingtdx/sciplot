{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-desktop",
  "description": "Allow the main SciPlot God desktop window to use core IPC and native file dialogs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
