# LabPlot Nextgen Workqueue

Current queue is complete. The durable product facts now live in:

- `docs/engineering-handoff.md`
- `docs/sidecar-api.md`
- `docs/macos-frontend-design.md`

Future LabPlot-informed work should start from those documents and create a new dated workqueue only when there are multiple ordered tasks that need persistent tracking.
